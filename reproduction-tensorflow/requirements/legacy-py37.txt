# Direct runtime dependencies for the original Python 3.7 / TensorFlow 1.14 code.
# TF and NumPy versions are confirmed by the historical result summaries.
# Other versions are compatible-era reconstruction pins, not a recovered lockfile.
tensorflow==1.14.0
numpy==1.18.1
scipy==1.3.3
scikit-learn==0.22.2.post1
statsmodels==0.11.1
matplotlib==3.1.3
seaborn==0.10.1
pandas==1.0.5
protobuf==3.20.3
h5py==2.10.0

